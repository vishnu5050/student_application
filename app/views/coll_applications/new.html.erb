<h1>New Application </h1>

<%= form_for(@coll_application) do |f| %>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
  <% if @coll_application.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@coll_application.errors.count, "error") %> prohibited this coll_application from being saved:</h2>

      <ul>
      <% @coll_application.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.label :student_id %>
    <%= f.collection_select :student_id, Student.all,:id,:name,prompt: "Select Your Name" ,class: 'dropdown-menu' %>
  <br>
  <br>
    <%= f.label :college_id %>
    <%= f.collection_select :college_id, College.all,:id,:name ,prompt: "Select the College",class: 'dropdown-menu' %>
  <br>
  <br>
    <%= f.submit "Post Application",class: "btn-btn-primary" %>
  
<% end %>

<%= link_to 'Back', coll_applications_path, class: 'btn-btn-primary' %>




